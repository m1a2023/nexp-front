"use client";

export const AllMoneyBoxTemplate = ({ styles }) => {
  return (
    <div className="absolute w-full h-full flex justify-center items-center backdrop-blur-lg backdrop-brightness-95 ">
      <div
        className={`w-[40%] h-[60%] bg-[#fff] rounded-2xl text-black flex justify-center items-center`}
      >
        <span>sosal?</span>
      </div>
    </div>
  );
};
